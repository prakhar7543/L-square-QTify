"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.useRender = useRender;
var _useRenderElement = require("../utils/useRenderElement");
/**
 * Returns an object with a `renderElement` function that renders a Base UI element.
 *
 * @public
 */
function useRender(params) {
  const {
    render,
    props,
    state,
    refs
  } = params;
  const {
    ref: intrinsicRefProp,
    ...intrinsicProps
  } = props || {};
  const renderElement = (0, _useRenderElement.useRenderElement)(undefined, {
    render
  }, {
    props: intrinsicProps,
    state,
    ref: [intrinsicRefProp, ...(refs || [])].filter(x => x != null),
    styleHooks: false
  });
  return {
    renderElement
  };
}