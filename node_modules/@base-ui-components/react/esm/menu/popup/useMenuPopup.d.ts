import type { FloatingEvents } from '@floating-ui/react';
import type { OpenChangeReason } from '../../utils/translateOpenChangeReason.js';
export declare function useMenuPopup(parameters: useMenuPopup.Parameters): useMenuPopup.ReturnValue;
export declare namespace useMenuPopup {
    interface Parameters {
        /**
         * The FloatingEvents instance of the menu's root.
         */
        menuEvents: FloatingEvents;
        /**
         * Callback to set the open state of the menu.
         */
        setOpen: (open: boolean, event: Event | undefined, reason: OpenChangeReason | undefined) => void;
    }
    type ReturnValue = void;
}
